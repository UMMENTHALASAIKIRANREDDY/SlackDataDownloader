# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Slack OAuth tokens (xoxp-... user tokens or xoxb-... bot tokens).
SLACK_ADMIN_TOKEN=
SLACK_USER_TOKEN=
# Optional: comma-separated additional user tokens for multi-token fetch.
SLACK_USER_TOKENS=
